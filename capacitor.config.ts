import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aprende-ionic-ngstyle-ngclass',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
